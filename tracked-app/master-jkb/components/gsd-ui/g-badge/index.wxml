<view class="badge component-class badge--{{dot?'dot':'normal'}} badge__position--{{position}}">
    <view wx:if="{{dot}}"></view>
    <view wx:else>
        <view wx:if="{{showOverflowCount}}"> {{overflowCount}}+ </view>
        <view wx:else> {{count}} </view>
    </view>
</view>
